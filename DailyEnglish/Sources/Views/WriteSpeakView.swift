import SwiftUI

struct WriteSpeakView: View {
    var body: some View {
        NavigationStack {
            Text("Write & Speak Practice")
                .navigationTitle("Write & Speak")
        }
    }
}
