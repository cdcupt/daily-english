import SwiftUI

struct VocabularyView: View {
    var body: some View {
        NavigationStack {
            Text("Vocabulary Practice")
                .navigationTitle("Vocabulary")
        }
    }
}
